version: '3.8'

services:
  timebank:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: timebank-india
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - ./data:/app/data
